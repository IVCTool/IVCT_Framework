dependencies {
	compile     libraries.slf4j_api
	compile		libraries.junit
	compile		libraries.hamcrest_core
}

javadoc {
			options.addStringOption('XDoclint:none', '-quiet')
			failOnError = false
		}
