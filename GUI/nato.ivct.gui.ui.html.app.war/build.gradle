apply plugin: "war"

apply from: "../../.shared/libraries.gradle"
apply from: "../../.shared/commons.gradle"

description = """IVCT HTML UI WAR"""

war {
    baseName = 'ivct.gui.ui.html'
	archiveName = "${baseName}##${version}.${extension}"
}

dependencies {
	implementation     project(':GUI:nato.ivct.gui.ui.html')
}
